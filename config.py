# Lab settings only — fixed secret for local demo, not for production.
SECRET_KEY = "eduvibe-insecure-secret-key"
UPLOAD_FOLDER = "static/uploads"  # Used by (A6) file upload at POST/PUT /admin/upload
